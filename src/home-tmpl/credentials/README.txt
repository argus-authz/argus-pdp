This directory should contain the X.509 credentials used by the PDP.

The private key should in the file 'service-key.pem' and contain the 
unecrypted, PEM-encoded, private key.

The certificate should in the file 'service-cert.pem' and contain the 
PEM-encoded certificate.
